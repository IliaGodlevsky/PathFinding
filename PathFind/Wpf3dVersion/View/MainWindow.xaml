<Window x:Class="Wpf3dVersion.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:converters="clr-namespace:Wpf3dVersion.Converters" 
        xmlns:viewmodel="clr-namespace:Wpf3dVersion.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="800">
    <Window.DataContext>
        <viewmodel:MainWindowViewModel></viewmodel:MainWindowViewModel>
    </Window.DataContext>
    <Window.Resources>
        <converters:LookBackConverter x:Key="lookBackConverter"></converters:LookBackConverter>
        <converters:ZoomConverter x:Key="ZConverter"></converters:ZoomConverter>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Expander Grid.Column="1" Grid.RowSpan="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                </Grid.RowDefinitions>
                <Slider Margin="5" MinWidth="100" Minimum="0" Maximum="1000" Grid.Row="0" Value="{Binding ElementName=look, Path=Position, Converter={StaticResource ZConverter}}"></Slider>
                <Slider Margin="5" MinWidth="100" Minimum="0" Maximum="360" Grid.Row="1" Value="{Binding ElementName=Rotate, Path=Angle}"></Slider>
                <Slider Margin="5" MinWidth="100" Minimum="0" Maximum="360" Grid.Row="2" Value="{Binding ElementName=Rotate2, Path=Angle}"></Slider>
                <Slider Margin="5" MinWidth="100" Minimum="0" Maximum="360" Grid.Row="3" Value="{Binding ElementName=Rotate3, Path=Angle}"></Slider>
            </Grid>
        </Expander>
        <ToolBar Grid.Row="0">
            <Button Command="{Binding LoadGraphCommand}">
                <Image Style ="{StaticResource ImageStyle}" Source="/Resources/Images/open.png"></Image>
            </Button>
            <Button Command="{Binding SaveGraphCommand}">
                <Image Style ="{StaticResource ImageStyle}" Source="/Resources/Images/save.png"></Image>
            </Button>
            <Button Command="{Binding CreateNewGraphCommand}">
                <Image Style ="{StaticResource ImageStyle}" Source="/Resources/Images/new.png"></Image>
            </Button>
            <Button Command="{Binding ClearGraphCommand}">
                <Image Style ="{StaticResource ImageStyle}" Source="/Resources/Images/clear.png"></Image>
            </Button>
            <Button Command ="{Binding StartPathFindCommand}">
                <Image Style ="{StaticResource ImageStyle}" Source="/Resources/Images/start.png"></Image>
            </Button>
            <Button Command ="{Binding ChangeOpacityCommand}">
            </Button>
        </ToolBar>
        <Viewport3D Grid.Row="2">
            <Viewport3D.Camera>
                <PerspectiveCamera x:Name="look" LookDirection="{Binding RelativeSource={RelativeSource Self}, 
                    Path=Position, Converter={StaticResource lookBackConverter}}" Position="600,600,600">
                    <PerspectiveCamera.Transform>
                        <RotateTransform3D>
                            <RotateTransform3D.Rotation>
                                <AxisAngleRotation3D x:Name="Rotate2" Axis="0 0 1" />
                            </RotateTransform3D.Rotation>
                        </RotateTransform3D>
                    </PerspectiveCamera.Transform>
                </PerspectiveCamera>
            </Viewport3D.Camera>
            <ModelVisual3D x:Name="mainVisual3D">
                <ModelVisual3D.Content>
                    <Model3DGroup>
                        <DirectionalLight Direction="{Binding ElementName=look, Path=LookDirection}" />
                    </Model3DGroup>
                </ModelVisual3D.Content>
                <ModelVisual3D x:Name="GraphField">
                    <ModelVisual3D.Transform>
                        <RotateTransform3D>
                            <RotateTransform3D.Rotation>
                                <AxisAngleRotation3D x:Name="Rotate3" Axis="0 1 0" />
                            </RotateTransform3D.Rotation>
                        </RotateTransform3D>
                    </ModelVisual3D.Transform>
                </ModelVisual3D>
                <ModelVisual3D.Transform>
                    <RotateTransform3D>
                        <RotateTransform3D.Rotation>
                            <AxisAngleRotation3D x:Name="Rotate" Axis="1 0 0" />
                        </RotateTransform3D.Rotation>
                    </RotateTransform3D>
                </ModelVisual3D.Transform>
            </ModelVisual3D>
        </Viewport3D>
        <StatusBar Grid.Row="3">
            <TextBlock Text = "{Binding PathFindingStatistics}"></TextBlock>
        </StatusBar>
        <StatusBar Grid.Row="1">
            <TextBlock Text="{Binding GraphParametres}"></TextBlock>
        </StatusBar>
    </Grid>
</Window>
